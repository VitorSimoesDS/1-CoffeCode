var multiplying = 10;
var multiplier = 1;

for (multiplier; multiplier <= 10; multiplier++) {
    console.log(multiplying + " multiplicador por " + multiplier + " é igual a: " + (multiplying*multiplier)); 
}
